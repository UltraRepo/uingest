
# MCP Server Configuration
HOST=0.0.0.0
PORT=8051
TRANSPORT=sse

# Optional path for Playwright browser automation
PLAYWRIGHT_BROWSERS_PATH=/Users/sc/Library/Caches/ms-playwright/

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key
OPENAI_API_BASE_URL=http://localhost:8080
EMBEDDING_MODEL_NAME=granite-embed-30m-eng

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=uingest
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_ORG_ID=your_openai_org_id_if_applicable # Uncomment if you use a specific OpenAI organization


# Optional: Specify Python path if needed for development,
# but typically not required when using a virtual environment correctly.
# PYTHONPATH=./src
